const LocationSchema = require('./location-schema');
const VerificationSchema = require('./verification-schema');
const CourseSchema = require('./course-schema');

module.exports = {
  LocationSchema,
  VerificationSchema,
  CourseSchema,
};
